@model IEnumerable<ASP_OnlineShopConnection.NhaSanXuat>
@{ Layout = null; }
<ul class="multi-column-dropdown">
    @foreach (var item in Model)
    {
        <li>
            <a href="women.html">
                @Html.ActionLink(item.TenNhaSanXuat, "Type", "Products", new { id = item.MaNhaSanXuat }, null)
            </a>
        </li>

    }
</ul>